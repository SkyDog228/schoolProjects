SELECT nazwa, ulica FROM kwiaciarnie WHERE kwiaciarnie.miasto='Malbork';

SELECT zamowienia.id_kwiaciarni, COUNT(zamowienia.id_zam) FROM zamowienia GROUP by zamowienia.id_kwiaciarni;

SELECT nazwa, miasto, zamowienia.kwiaty FROM kwiaciarnie 
JOIN zamowienia ON kwiaciarnie.id_kwiaciarni=zamowienia.id_kwiaciarni WHERE zamowienia.data = '2017-01-07';


CREATE TABLE klienci (
    id INT unsigned PRIMARY KEY AUTO_INCREMENT NOT NULL,
    imie VARCHAR(15),
    nazwisko VARCHAR(15),
    rabat INT unsigned
)
